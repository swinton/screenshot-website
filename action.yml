name: 'screenshot-url'
description: 'Take a screenshot of a URL'
inputs:
  source:
    description: 'Source of the content to be captured, may be a URL or HTML content'
    required: true
  destination:
    description: 'The destination filename the captured screenshot will be written to'
    required: false
    default: 'screenshot.png'
  full-page:
    description: 'Capture the full scrollable page, not just the viewport'
    required: false
    default: 'true'
outputs:
  path:
    description: 'The filesystem path to the generated screenshot'
runs:
  using: 'node12'
  main: 'dist/index.js'
